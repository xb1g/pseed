/**
 * Re-export the refactored MapViewer component
 * This maintains backwards compatibility while using the new modular structure
 */

export { MapViewer as MapViewerWithProvider } from "./MapViewer";