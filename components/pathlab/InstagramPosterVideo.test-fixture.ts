/** Pure numbers — extracted from InstagramPosterVideo.tsx so the
 *  test file can import them without pulling in React, mediabunny,
 *  or html-to-image. If you change these in the component, update
 *  them here too.
 */
export const FPS = 24;
export const DURATION_SEC = 12;
export const TOTAL_FRAMES = 288;
export const WIDTH = 1080;
export const HEIGHT = 1350;