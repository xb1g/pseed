/**
 * Manual Worker Trigger Script
 *
 * NOTE: This script is deprecated as cron jobs have been removed.
 * The worker endpoint no longer exists. This file is kept for reference only.
 *
 * Previous usage:
 *   npx tsx scripts/trigger-worker.ts
 *   npx tsx scripts/trigger-worker.ts --production
 *   npx tsx scripts/trigger-worker.ts --loop (keeps running)
 */

console.log('⚠️  This script is deprecated. The cron job system has been removed.');
console.log('The /api/direction/worker endpoint no longer exists.');
console.log('Please use the new scheduling mechanism instead.');
